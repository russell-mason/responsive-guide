export interface Viewport {
    name: string;

    device: string;

    orientation: string;

    deviceWidth: number;

    deviceHeight: number;

    viewWidth: number;

    viewHeight: number;

    className: string;
}